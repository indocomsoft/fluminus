language: elixir

sudo: false

elixir:
  - 1.9

otp_release:
  - 21.3
  - 22.0

cache:
  directories:
    - _build
    - deps

env: MIX_ENV=test

script:
  - mix coveralls.travis
  - mix credo
  - mix format --check-formatted
